package com.dangercode.javaCore.interfaces.edgequestion;

public class ClassA extends AbstractClassA {
    // If we hadn't added this layer between InterfaceA and ClassA,
    // we would have been forced to override the methods of InterfaceA directly here.
}
